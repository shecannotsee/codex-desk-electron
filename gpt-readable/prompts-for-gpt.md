# 给 GPT 的高效提问模板

## 1. 改功能

请在不改动行为的前提下，修改 `<文件路径>` 的 `<函数名>`，目标是：`<目标>`。
要求：
1. 说明影响的调用链
2. 给出最小变更补丁
3. 列出回归测试步骤

## 2. 排查问题

请定位 `<现象>` 的根因。
上下文：
- 入口：`<入口按钮/IPC>`
- 相关文件：`<文件1> <文件2>`
请按“可能性从高到低”给出原因与验证方法。

## 3. 做重构

请把 `<大文件路径>` 按职责拆分为 `<目标模块数>` 个模块。
要求：
1. 对外接口不变
2. 保持现有行为
3. 输出拆分后的代码地图与迁移说明
