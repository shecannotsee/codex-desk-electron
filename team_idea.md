# Conductor

多智能体协作系统，通过分层编排驱动复杂软件项目的完整开发生命周期。

---

## 核心理念

将多个 Claude Code / Codex CLI 实例组织成分层协作集群。每个实例承担特定角色，通过本地文件系统共享状态，由 Orchestrator 统一调度，Verifier 客观验证产物。

系统不依赖任何 CLI 实例的会话记忆，所有状态持久化于本地文件。

---

## 系统架构

```
┌─────────────────────────────────────┐
│           Orchestrator              │
│   任务分解 / DAG 管理 / 任务分发       │
└──────────────┬──────────────────────┘
               │ 结构化任务描述（JSON）
       ┌───────┴────────┐
       │                │
  ┌────▼────┐      ┌────▼────┐
  │ Worker  │      │ Worker  │   实际执行
  │  CLI A  │      │  CLI B  │
  └────┬────┘      └────┬────┘
       └───────┬────────┘
               │ 产物输出
  ┌────────────▼────────────────────┐
  │          Verifier CLI           │
  │      客观验证（不依赖自报）        │
  └────────────┬────────────────────┘
               │
  ┌────────────▼────────────────────┐
  │        Shared State Store       │
  │         （本地文件系统）          │
  └─────────────────────────────────┘
```

---

## 角色定义

| 角色 | 职责 |
|------|------|
| Orchestrator | 接收高层目标，分解为任务 DAG，分发给 Worker |
| Architect | 负责整体设计、模块划分、接口定义 |
| Worker | 执行具体任务（实现、测试、文档等） |
| Integrator | 负责模块集成与集成验证 |
| Verifier | 客观验证产物，融合监督职能，是分工的一部分 |

---

## 核心设计原则

**1. 无持久记忆**
实例无状态，每次启动从文件读取上下文，不依赖会话记忆。

**2. 文件系统作为消息总线**
之间不直接通信，通过读写 `shared_state/` 交换信息，天然支持异步与并发。

**3. 上下文裁剪**
每个只接收其任务所需的最小上下文，避免上下文窗口溢出。

**4. 客观验证**
Verifier 通过实际运行验证脚本检验产物，不接受 Worker 自我报告作为完成依据。

**5. 幂等任务**
任务可重试，失败后重新执行不产生副作用。

**6. DAG 依赖管理**
维护任务有向无环图，防止循环依赖与死锁。

**7. 能力注册表**
记录每个实例的能力边界，Orchestrator 按能力匹配分配任务，而非随机分发。

**8. 冲突仲裁**
同一资源同一时刻只允许一个 Worker 持有写权限，通过 DAG 依赖关系静态避免冲突。

---

## 待解决问题

- [ ] Orchestrator 任务分解的粒度标准
- [ ] 实例的启动与生命周期管理（手动 / 脚本自动化）
- [ ] 能力注册表的初始化与更新机制
- [ ] 异构 CLI 的能力差异处理（Claude Code vs Codex CLI）
- [ ] 验证脚本的通用化设计（适配不同类型项目）
